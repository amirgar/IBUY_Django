meta:
  environment: python
  toolchain:
    name: pip
    version: 3.11
build:
  requirementsPath: requirements/prod.txt
run:
   command: cd myshop && gunicorn -b 127.0.0.1:8080 myshop.wsgi:application
   containerPort: 5000  